{
  "name": "mdb",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "boot": "docker run --rm --name my-mongo-db -dp 27017:27017 -v mongo-data:/data/db mongo",
    "shutdown": "docker stop my-mongo-db"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^10.0.0",
    "mongoose": "^5.13.7"
  }
}
